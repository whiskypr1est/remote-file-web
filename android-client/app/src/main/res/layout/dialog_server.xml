<?xml version="1.0" encoding="utf-8"?>
<!--
  服务器地址对话框。
  ★ 提示里专门写了「只填 IP 会自动补 http:// 与 :8000」——
    因为最常见的输入就是 `192.168.1.100`，不该逼用户把 scheme 与端口敲全。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="10dp"
    android:paddingBottom="2dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="4dp"
        android:text="@string/label_server"
        android:textSize="13sp" />

    <EditText
        android:id="@+id/server_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/dialog_hint"
        android:imeOptions="actionDone"
        android:inputType="textUri"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:textSize="15sp" />

    <CheckBox
        android:id="@+id/keep_awake"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/label_keep_awake"
        android:textSize="13sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:alpha="0.55"
        android:text="@string/tip_back"
        android:textSize="11sp" />

</LinearLayout>
